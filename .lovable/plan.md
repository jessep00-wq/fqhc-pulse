No code changes — this was a read-only question. See the chat reply above for the exact redirect behavior of `src/pages/Auth.tsx` and `src/components/ProtectedRoute.tsx`. Approve to switch to build mode if you want follow-up edits.