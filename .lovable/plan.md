In `index.html`, remove the static HTML fallback (`<div class="mw-fallback">` and all its contents) so that only `<div id="root"></div>` remains. No other part of the file is touched — all `<head>` content, `<style>`, `<noscript>`, `<script>`, and JSON-LD blocks stay exactly as they are.